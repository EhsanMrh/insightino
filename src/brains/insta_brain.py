class InstaBrain:
    FACTS_PATH = "data/marts/media_facts.parquet"
    def ingest_dump(self, src_path: str) -> str: ...
    def map_products(self, rules: dict) -> int: ...
    def build_features(self, overwrite=False) -> str: ...
    def query(self, product_id: str|None, start: str, end: str): ...